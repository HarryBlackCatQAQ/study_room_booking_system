server {
    listen 80;
    server_name _;

    # keep the frontend build output as the document root
    root /usr/share/nginx/html;
    index index.html;

    # keep react router history mode working behind nginx
    location / {
        try_files $uri $uri/ /index.html;
    }
}
