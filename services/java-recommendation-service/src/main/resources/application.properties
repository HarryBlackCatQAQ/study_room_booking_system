# set the spring application name for logs and spring metadata
spring.application.name=java-recommendation-service
# run the grpc server on the port used by the django backend
spring.grpc.server.port=50061
# keep service logs at info level by default
logging.level.com.studynest=INFO

