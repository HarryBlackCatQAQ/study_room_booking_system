# central place for the main api url prefixes
class ApiPrefix:
    # auth endpoints prefix
    AUTH = "api/auth/"
    # room endpoints prefix
    ROOMS = "api/rooms/"
    # booking endpoints prefix
    BOOKINGS = "api/bookings/"
    # review endpoints prefix
    REVIEWS = "api/reviews/"
    # support endpoints prefix
    SUPPORT = "api/support/"
    # smart service endpoints prefix
    SMART = "api/smart/"
