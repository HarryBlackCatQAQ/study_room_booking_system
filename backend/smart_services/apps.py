from django.apps import AppConfig


# app configuration for the smart services module
class SmartServicesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'smart_services'
